# OPTIONAL: metered OpenRouter proxy ("Openwork Models"). Needs
# OPENROUTER_MANAGEMENT_API_KEY, OPENROUTER_WORKSPACE_ID and Stripe keys.
ARG DEN_VERSION=0.18.40
FROM ghcr.io/different-ai/openwork-inference:${DEN_VERSION}
EXPOSE 8791
